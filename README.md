# Awraaq Generator

A simple Django-based project for generating formatted Awraaq PDFs and booklets.

### Setup Instructions

1. Create a virtual environment:
   ```bash
   python -m venv .venv
   source .venv/bin/activate   # Windows: .venv\Scripts\activate

commit - 001: static fonts were not configured properly, fixed it and now pushing it on github
uploaded on github and fixed fonts not loading correctly. the page layout seems nice. in the coming commits im going to install a few packages that will help us with the functionality we desire that is similar to microsoft word
